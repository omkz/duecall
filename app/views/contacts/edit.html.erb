<% content_for :title, "Edit #{@contact.name} · DueCall" %>

<div class="mx-auto max-w-xl">
  <%= link_to "← Contact", contact_path(@contact), class: "text-sm font-medium text-slate-500 hover:text-slate-900" %>
  <h1 class="mt-3 text-3xl font-bold tracking-tight text-slate-950">Edit contact</h1>

  <div class="mt-8 rounded-xl border border-slate-200 bg-white p-6 shadow-sm">
    <%= render "form", customer: @customer, contact: @contact %>
  </div>
</div>
