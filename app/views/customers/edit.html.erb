<% content_for :title, "Edit #{@customer.name} · DueCall" %>

<div class="mx-auto max-w-xl">
  <%= link_to "← Customer", customer_path(@customer), class: "text-sm font-medium text-slate-500 hover:text-slate-900" %>
  <h1 class="mt-3 text-3xl font-bold tracking-tight text-slate-950">Edit customer</h1>

  <div class="mt-8 rounded-xl border border-slate-200 bg-white p-6 shadow-sm">
    <%= render "form", customer: @customer %>
  </div>
</div>
