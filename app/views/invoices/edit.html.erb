<% content_for :title, "Edit #{@invoice.number} · DueCall" %>

<div class="mx-auto max-w-xl">
  <%= link_to "← Invoice", invoice_path(@invoice), class: "text-sm font-medium text-slate-500 hover:text-slate-900" %>
  <h1 class="mt-3 text-3xl font-bold tracking-tight text-slate-950">Edit invoice</h1>

  <div class="mt-8 rounded-xl border border-slate-200 bg-white p-6 shadow-sm">
    <%= render "form", customer: @customer, invoice: @invoice %>
  </div>
</div>
